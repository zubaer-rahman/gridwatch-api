model AuditLog {
  id           String @id @default(uuid())
  actorId      String
  action       String
  entity       String
  entityId     String
  previousData Json?
  newData      Json?

  createdAt DateTime @default(now())

  @@index([entityId])
  @@index([createdAt])
  @@map("audit_logs")
}
